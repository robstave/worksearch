# Root environment template (safe to commit)
# Copy to `.env` and fill in values.

# --- Postgres (used by docker-compose) ---
POSTGRES_USER=worksearch
POSTGRES_PASSWORD=change-me
POSTGRES_DB=worksearch
POSTGRES_PORT=5432

# --- Ports (optional) ---
# Host ports for docker-compose published ports.
API_PORT=3000
WEB_PORT=5173

# --- API (api container) ---
# docker-compose.yml composes DATABASE_URL internally from POSTGRES_*.
# You only need APP_SECRET + FRONTEND_ORIGIN here.

# Used for signing/encrypting auth/session data (cookie auth).
APP_SECRET=change-me-in-prod

# Frontend origin allowed to send cookie-auth requests in development.
FRONTEND_ORIGIN=http://localhost:5173

# --- Web (web container) ---
# Base URL the web app uses to reach the API.
VITE_API_BASE=http://localhost:3000/api
