# API environment template (safe to commit)
# Copy to `.env` (in this folder) and fill in values.

NODE_ENV=development
PORT=3000

# Postgres connection string
# Example: postgres://user:password@localhost:5432/worksearch
DATABASE_URL=postgres://worksearch:change-me@localhost:5432/worksearch

# Session/auth secret (cookie auth). Use a long random string in real envs.
APP_SECRET=change-me

# Optional: used for CORS (dev) when frontend talks to API with cookie auth.
FRONTEND_ORIGIN=http://localhost:5173
