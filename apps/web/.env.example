# Web environment template (safe to commit)
# Copy to `.env` (in this folder) and fill in values.

# Where the browser should reach the API.
# If you proxy through Vite, you may not need this.
VITE_API_BASE=http://localhost:3000/api
